# BLE bench record — 2026-08-22 (Builder session, Felipe's hands) [REAL]

Device: `ff1f7d2` (BLE build) on app1, cable unplugged, battery 100 → 90 % over the ~15 min of BLE + Wi-Fi (modem sleep) + display + streaming. Evidence: `sticks3/.device-backups/logs/capture-20260821T230348/raw.log` (sanctioned `--tcp` capture), `serial-stepC-ff1f7d2-*.log`.

| PLAN-r1 Step 5 item | Result |
| --- | --- |
| 5.1 Pairing | `hold 2 s` in locked MOUSE with no transport → `EVENT,BLE,ADVERTISING,PAIRING,seconds=60` → Mac connect → `CONNECTED,output=LOCKED` → `SECURE,bonded=1,output=LOCKED`; STATUS `ble=SECURE,hid=BLE,bonded=1`, still locked (`LOCKED,reason=TRANSPORT_TOPOLOGY` on selection) — PASS |
| 5.2 Locked negative | Not separately observed by the operator (device was locked and secure for minutes; no cursor complaint) — record as not explicitly tested |
| 5.3 BLE pointer | `hold 2 s` → `EVENT,MOUSE_OUTPUT,ARMED,hid=BLE`; operator: "left/right very good, up/down not that much"; "perfect" after reconnect — PASS (vertical feel = tuning follow-up, `kVerticalGyroAxis=0/kVerticalSign=1`, deadzone 1.4 dps, 18 px/deg) |
| 5.4 Physical lock | `hold 2 s` → `LOCKED,reason=PHYSICAL` — PASS |
| 5.5 Disconnect / reconnect | Mac Bluetooth off while armed → `LOCKED,reason=TRANSPORT_TOPOLOGY` + `BLE,DISCONNECTED,reason=531`; no self-advertising (STATUS `ble=IDLE,hid=NONE,bonded=1`); `hold 2 s` → `ADVERTISING,RECONNECT,seconds=30` → `CONNECTED` → `SECURE,bonded=1`, still locked; `hold 2 s` → armed, cursor moves — PASS. Unknown-peer rejection and the 5 s forget+re-pair flow not exercised |
| 5.6 USB coexistence switch | NOT RUN (no cable available at the end of the session) — tomorrow |
| 5.7 TCP + BLE RF coexistence | Three recorded 30 s `COEX_IMU` windows while armed over BLE with the TCP mirror attached: 165.8 / 165.5 / 164.8 Hz, p50 5.00 / 4.99 / 4.98 ms, p95 7.12 / 7.40 / 7.90 ms, p99 31.09 / 31.11 / 31.04 ms, max 34.94 / 36.24 / 36.67 ms, BLE reports 1571 / 1284 / 115, failures 0, rows missing 0, `DROPPED` 0 (cumulative since boot). Baseline 2026-08-18 (70 s block): 170.3 Hz, p50 4.98, p95 6.99, p99 28.07, max 30.94 ms. Rate −2.6…−3.2 % (≤ 5 % ✓), p99 +3.0 ms (< 5 ms ✓), max +4.0…+5.7 ms (the plan asks to investigate "a worse maximum": within the ~35 ms class already seen on TCP; flagged for the Architect, not a fail). A first unrecorded window (live monitor only): 23.1 s, 3819 samples, 966 reports, max 33.9 ms |
| 5.8 Battery | 100 % at boot (on USB until then) → 98 % at 494 s → 90 % at 922 s of the capture (~1.1 %/min while streaming + BLE + Wi-Fi + display); observation only |
| Soak | ~25 min connected, four armed windows, 4945 BLE reports, 0 failures, heap 112–137 KB, no reset; the dedicated 10-minute armed soak is still to run |
| Step 6 Stage 2 (camera click) | NOT RUN — Felipe's next session |

Observations: a mid-session second `EVENT,BLE,SECURE` (re-encryption by the Mac) kept the armed state; the literal `output=LOCKED` in that event text is a static label (cosmetic). Pointer feel asymmetry (vertical weaker) is a motion-config tuning item, not a transport issue — both axes share the 8 ms BLE accumulation and ±60 clamp.
